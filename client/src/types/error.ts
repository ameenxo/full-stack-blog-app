export interface AuthError {
    error: boolean;
    message: string;
}
export interface BlogFetchError {
    error: boolean;
    message: string;
}
export interface RegisterError {
    error: boolean,
    message: string    
}