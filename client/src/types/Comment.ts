export interface Comment {
    user: string;  // User ID of the commenter
    text: string;  // Comment text
    _id: string;   // Unique ID of the comment
    createdAt: string; // Timestamp of the comment
}